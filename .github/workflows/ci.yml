name: CI
on: [push, pull_request]
jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: backend build/test steps
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: frontend build/test steps
